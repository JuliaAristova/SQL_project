Functions may be nested within each other so that the output of one function 
serves as the input parameter of another.

When one function is nested withing another, original function becomes nested.
Most inner function is executed first.
Most outer function is executed last.
